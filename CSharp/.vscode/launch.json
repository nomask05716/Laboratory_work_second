{
    "version": "0.2.0",
    "configurations": [
      {
        "name": ".NET Core Launch (console)",
        "type": "coreclr",
        "request": "launch",
        "preLaunchTask": "build", // Ссылается на задачу в tasks.json
        "program": "${workspaceFolder}/bin/Release/net9.0/linux-x64/CSharp",
        "args": [],
        "cwd": "${workspaceFolder}",
        "console": "integratedTerminal", // Для ввода/вывода
        "stopAtEntry": false
      }
    ]
  }